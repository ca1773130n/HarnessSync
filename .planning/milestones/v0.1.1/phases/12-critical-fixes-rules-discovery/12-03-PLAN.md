---
phase: 12-critical-fixes-rules-discovery
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified: [tests/test_phase12_integration.py]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "Integration test verifies Codex adapter writes config.toml with on-request approval policy"
    - "Integration test verifies Gemini adapter writes tools.exclude and tools.allowed (not blockedTools/allowedTools)"
    - "Integration test verifies OpenCode adapter writes permission (singular) with per-tool entries"
    - "Integration test verifies SourceReader discovers rules files from nested directories"
    - "Integration test verifies frontmatter paths: parsing produces correct scope_patterns"
    - "No deprecated keys/values remain anywhere in src/"
  artifacts:
    - path: "tests/test_phase12_integration.py"
      provides: "Integration tests for all phase 12 fixes"
  key_links:
    - from: "tests/test_phase12_integration.py"
      to: "src/adapters/codex.py"
      via: "Tests Codex sync_settings output"
      pattern: "CodexAdapter"
    - from: "tests/test_phase12_integration.py"
      to: "src/adapters/gemini.py"
      via: "Tests Gemini sync_settings output"
      pattern: "GeminiAdapter"
    - from: "tests/test_phase12_integration.py"
      to: "src/adapters/opencode.py"
      via: "Tests OpenCode sync_settings output"
      pattern: "OpenCodeAdapter"
    - from: "tests/test_phase12_integration.py"
      to: "src/source_reader.py"
      via: "Tests rules directory discovery"
      pattern: "SourceReader"
---

<objective>
Verify all phase 12 fixes with integration tests and perform final codebase-wide sweep for deprecated patterns.

Purpose: Ensure all adapter fixes and rules discovery work correctly end-to-end. Catch any remaining deprecated patterns that were missed.

Output: Integration test file and clean codebase with zero deprecated patterns.
</objective>

<execution_context>
@.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.5/references/execute-plan.md
@.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.5/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v0.1.1/phases/12-critical-fixes-rules-discovery/12-RESEARCH.md
@.planning/milestones/v0.1.1/phases/12-critical-fixes-rules-discovery/12-01-SUMMARY.md
@.planning/milestones/v0.1.1/phases/12-critical-fixes-rules-discovery/12-02-SUMMARY.md
@src/adapters/codex.py
@src/adapters/gemini.py
@src/adapters/opencode.py
@src/source_reader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for all phase 12 changes</name>
  <files>tests/test_phase12_integration.py</files>
  <action>
    Create tests/test_phase12_integration.py with the following test cases using pytest and tmp_path fixtures:

    **Codex tests:**
    1. test_codex_config_filename: Assert CONFIG_TOML == "config.toml"
    2. test_codex_approval_policy_auto: Create CodexAdapter with tmp_path, call sync_settings({'approval_mode': 'auto', 'permissions': {}}). Read the output config.toml file. Assert it contains 'on-request' and NOT 'on-failure'.
    3. test_codex_approval_policy_ask: Same but with approval_mode='ask'. Assert 'on-request'.

    **Gemini tests:**
    4. test_gemini_deny_list_uses_exclude: Create GeminiAdapter with tmp_path (ensure .gemini/ dir exists). Call sync_settings({'permissions': {'deny': ['Write'], 'allow': []}}). Read output settings.json. Assert 'exclude' key exists, 'blockedTools' does NOT exist.
    5. test_gemini_allow_list_uses_allowed: Same with allow list. Assert 'allowed' key exists, 'allowedTools' does NOT exist.

    **OpenCode tests:**
    6. test_opencode_permission_singular: Create OpenCodeAdapter with tmp_path. Call sync_settings({'permissions': {'deny': ['Write'], 'allow': ['Read']}}). Read output opencode.json. Assert 'permission' (singular) key exists. Assert 'permissions' (plural) key does NOT exist.
    7. test_opencode_bash_patterns: Call sync_settings with allow list containing 'Bash(git commit:*)'. Assert output has permission.bash dict with pattern entry.
    8. test_opencode_removes_old_permissions: Pre-write opencode.json with old {"permissions": {"mode": "default"}}. Call sync_settings(). Assert old 'permissions' key is gone, new 'permission' key exists.

    **SourceReader tests:**
    9. test_rules_discovery_project: Create tmp_path/.claude/rules/test.md with content "# Test rule". Create SourceReader with scope='project', project_dir=tmp_path. Call get_rules_files(). Assert returns 1 entry with correct content.
    10. test_rules_discovery_nested: Create tmp_path/.claude/rules/subdir/nested.md. Assert get_rules_files() finds it.
    11. test_rules_frontmatter_paths: Create .claude/rules/scoped.md with "---\npaths: src/**/*.ts\n---\nScoped rule". Assert scope_patterns == ["src/**/*.ts"].
    12. test_rules_frontmatter_list: Create rule with "---\npaths:\n  - a\n  - b\n---\nContent". Assert scope_patterns == ["a", "b"].
    13. test_rules_no_frontmatter: Create rule without frontmatter. Assert scope_patterns == [].
    14. test_rules_user_scope: Create cc_home/rules/user.md. Create SourceReader with scope='user', cc_home=cc_home_path. Assert get_rules_files() finds it with scope='user'.

    Include `from __future__ import annotations` at top of test file.
  </action>
  <verify>
    python -m pytest tests/test_phase12_integration.py -v
    # All tests should pass
    (Level 2: Proxy)
  </verify>
  <done>
    All 14 integration tests pass, covering every requirement in phase 12.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final codebase sweep for deprecated patterns</name>
  <files>src/adapters/codex.py, src/adapters/gemini.py, src/adapters/opencode.py</files>
  <action>
    Run a comprehensive grep across the entire src/ directory for ALL deprecated patterns. Fix any remaining occurrences:

    1. `grep -rn "codex\.toml" src/` -- should be zero matches
    2. `grep -rn "'on-failure'" src/` -- should be zero matches
    3. `grep -rn "blockedTools\|allowedTools" src/` -- should be zero matches (excluding cc2all_sync.py which is dead code)
    4. `grep -rn "permissions_config\[.mode.\]" src/` -- should be zero matches
    5. `grep -rn "existing_config\['permissions'\]" src/` -- should be zero matches in opencode.py (plural key writes)
    6. `grep -rn "Path\.home().*\.claude" src/` -- should be zero matches (all should use self.cc_home)

    For any matches found: fix them. If cc2all_sync.py has deprecated patterns, note in summary that it's dead code (per MEMORY.md) but consider fixing for consistency.

    Also verify that the project root `codex.toml` file (evidence of the filename bug) is noted -- the adapter fix means new syncs will write to `config.toml`, but the old `codex.toml` will remain as an orphan. Do NOT delete it (user may have customized it), but note the situation in the summary.
  </action>
  <verify>
    grep -rn "codex\.toml" src/ | grep -v "cc2all_sync"  # Zero matches outside dead code
    grep -rn "'on-failure'" src/                           # Zero matches
    grep -rn "blockedTools\|allowedTools" src/ | grep -v "cc2all_sync"  # Zero matches outside dead code
    (Level 1: Sanity)
  </verify>
  <done>
    Zero deprecated patterns remain in active source code (src/ excluding dead cc2all_sync.py).
    All adapter outputs match current official documentation formats.
    Orphan codex.toml situation documented.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- No deprecated keys/values in active source code
- All test files import correctly

Level 2 (Proxy):
- All 14 integration tests pass
- Each adapter produces correct output format when tested with sample input

Level 3 (Deferred):
- Full end-to-end sync with real configs accepted by target CLIs
</verification>

<success_criteria>
All integration tests pass.
Zero deprecated patterns remain in active codebase.
Phase 12 requirements RULES-01 through OC-09 are verified.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.1.1/phases/12-critical-fixes-rules-discovery/12-03-SUMMARY.md`
</output>
