---
phase: 12-critical-fixes-rules-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/adapters/codex.py, src/adapters/gemini.py, src/adapters/opencode.py]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "Codex adapter writes approval_policy = 'on-request' (never 'on-failure')"
    - "Codex adapter uses CONFIG_TOML = 'config.toml' (never 'codex.toml')"
    - "Gemini adapter writes tools.exclude (never tools.blockedTools) and tools.allowed (never tools.allowedTools)"
    - "OpenCode adapter writes 'permission' (singular) with per-tool allow/ask/deny values (never 'permissions' plural with mode)"
    - "OpenCode adapter maps Claude Code Bash tool patterns to permission.bash dict with wildcard patterns"
  artifacts:
    - path: "src/adapters/codex.py"
      provides: "Fixed Codex adapter with correct config filename and approval policy"
      contains: "config.toml"
    - path: "src/adapters/gemini.py"
      provides: "Fixed Gemini adapter with v2 tools config keys"
      contains: "exclude"
    - path: "src/adapters/opencode.py"
      provides: "Rewritten OpenCode permission sync with per-tool format"
      contains: "permission"
  key_links:
    - from: "src/adapters/codex.py"
      to: "src/utils/toml_writer.py"
      via: "TOML writing for config.toml"
      pattern: "write_toml_atomic"
    - from: "src/adapters/opencode.py"
      to: "src/utils/paths.py"
      via: "JSON writing for opencode.json"
      pattern: "write_json_atomic"
---

<objective>
Fix all three adapter outputs to match current official documentation for Codex, Gemini CLI, and OpenCode.

Purpose: Codex, Gemini, and OpenCode adapters currently emit deprecated or invalid configuration keys/values that target CLIs ignore or reject. These fixes ensure synced configs are actually loaded by the target tools.

Output: Three corrected adapter files producing valid configuration for their respective CLIs.
</objective>

<execution_context>
@.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.5/references/execute-plan.md
@.claude-personal2/plugins/cache/claude-plugin-marketplace/grd/0.3.5/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v0.1.1/phases/12-critical-fixes-rules-discovery/12-RESEARCH.md
@src/adapters/codex.py
@src/adapters/gemini.py
@src/adapters/opencode.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Codex adapter config filename and approval policy</name>
  <files>src/adapters/codex.py</files>
  <action>
    Two changes in src/adapters/codex.py:

    1. Line 39: Change `CONFIG_TOML = "codex.toml"` to `CONFIG_TOML = "config.toml"`.
       This is the official Codex CLI config filename per https://developers.openai.com/codex/config-reference.

    2. Line 453: Change `approval_policy = 'on-failure'` to `approval_policy = 'on-request'`.
       'on-failure' is NOT a valid Codex approval_policy value. Valid values are: untrusted, on-request, never, reject.

    3. Update the module docstring (line 10) to say "config.toml" instead of any mention of "codex.toml".

    4. IMPORTANT (narrow fix prevention): After making these changes, grep the ENTIRE codebase for:
       - Any remaining references to "codex.toml" (file content, not filesystem paths to existing files)
       - Any remaining references to "on-failure" as an approval policy value
       Fix ALL occurrences found, not just the ones in codex.py.

    Note on line 348 (user_path = self.project_dir / CONFIG_TOML): This writes user-scope config to the project root. The research notes this may be wrong (should be ~/.codex/config.toml), but changing the path logic is out of scope for this requirement (CDX-08 only covers the filename). Leave the path as-is but the filename will now correctly be config.toml.
  </action>
  <verify>
    grep -n "codex.toml" src/adapters/codex.py  # Should return NO matches
    grep -n "on-failure" src/adapters/codex.py   # Should return NO matches
    grep -rn "codex\.toml" src/                  # Should return NO matches in source code
    grep -n "config.toml" src/adapters/codex.py  # Should show CONFIG_TOML constant
    (Level 1: Sanity)
  </verify>
  <done>
    CONFIG_TOML == "config.toml" and approval_policy uses 'on-request' for auto mode.
    No references to "codex.toml" or "on-failure" remain in src/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Gemini adapter tools config keys and rewrite OpenCode permission format</name>
  <files>src/adapters/gemini.py, src/adapters/opencode.py</files>
  <action>
    **Gemini adapter (src/adapters/gemini.py):**

    1. Line 485: Change `tools_config['blockedTools'] = deny_list` to `tools_config['exclude'] = deny_list`.
    2. Line 492: Change `tools_config['allowedTools'] = allow_list` to `tools_config['allowed'] = allow_list`.
    3. Update the module docstring (line 11) to say "tools.exclude/tools.allowed" instead of "tools.blockedTools/allowedTools".
    4. Update the sync_settings docstring (line 457) similarly.

    **OpenCode adapter (src/adapters/opencode.py):**

    Rewrite sync_settings() (lines 372-430+) to use the new per-tool `permission` (singular) format instead of `permissions` (plural) with `mode`.

    The new format uses `permission` key with per-tool entries using `allow`/`ask`/`deny` values. The tool identifiers are: read, edit, glob, grep, list, bash, task, skill, lsp, todoread, todowrite, webfetch, websearch, codesearch, external_directory.

    Implementation:
    a. Create a tool name mapping dict:
       - Bash -> bash, Read -> read, Write -> edit, Edit -> edit, Glob -> glob, Grep -> grep
       - WebFetch -> webfetch, WebSearch -> websearch, TodoWrite -> todowrite, TodoRead -> todoread

    b. Process deny_list: For each tool in deny_list, map to OpenCode identifier and set to "deny".

    c. Process allow_list: For each tool in allow_list:
       - If it starts with "Bash(" and ends with ")", extract the command pattern.
         E.g., "Bash(git commit:*)" -> pattern "git commit *" (replace ":" with " ").
         Add to permission.bash as a dict: {"git commit *": "allow"}.
       - Otherwise, map tool name to OpenCode identifier and set to "allow".

    d. If bash has mixed dict entries and no default, set a default "*": "ask" entry.

    e. Write `permission` (singular) key to config. If old `permissions` (plural) key exists in existing_config, DELETE it to prevent ambiguity.

    f. Update module docstring (line 11) to say "permission (singular) with per-tool allow/ask/deny" instead of "permissions with conservative mapping".

    IMPORTANT (narrow fix prevention): After changes, grep the ENTIRE codebase for:
    - Any remaining "blockedTools" or "allowedTools" references in src/
    - Any remaining "permissions" (plural) key writes in src/adapters/ (not reads from Claude Code settings, which legitimately use 'permissions')
  </action>
  <verify>
    grep -n "blockedTools\|allowedTools" src/adapters/gemini.py  # Should return NO matches
    grep -n "'exclude'" src/adapters/gemini.py                    # Should show the fix
    grep -n "'allowed'" src/adapters/gemini.py                    # Should show the fix
    grep -n "permissions_config\|'permissions'" src/adapters/opencode.py  # Should NOT show writes of plural 'permissions' key
    grep -n "'permission'" src/adapters/opencode.py               # Should show singular key usage
    python -c "from src.adapters.opencode import OpenCodeAdapter; print('import ok')"
    python -c "from src.adapters.gemini import GeminiAdapter; print('import ok')"
    (Level 2: Proxy)
  </verify>
  <done>
    Gemini adapter writes tools.exclude and tools.allowed (v2 keys).
    OpenCode adapter writes permission (singular) with per-tool allow/ask/deny values.
    OpenCode maps Bash patterns to permission.bash wildcard entries.
    Old permissions (plural) key is removed from output when present.
    No references to deprecated keys remain in src/adapters/.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- CONFIG_TOML constant equals "config.toml"
- No occurrence of 'on-failure' in codex adapter
- No occurrence of 'blockedTools' or 'allowedTools' in gemini adapter
- No occurrence of 'permissions' (plural) key writes in opencode adapter

Level 2 (Proxy):
- All three adapter modules import without error
- Codex sync_settings() with approval_mode='auto' produces 'on-request'
- Gemini sync_settings() with deny list produces 'exclude' key
- OpenCode sync_settings() with allow list produces 'permission' (singular) with per-tool entries
- OpenCode sync_settings() with Bash patterns produces permission.bash dict

Level 3 (Deferred):
- Full sync produces valid config files accepted by each target CLI
</verification>

<success_criteria>
All three adapters produce configuration matching current official documentation.
Zero deprecated keys or values in adapter output code.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.1.1/phases/12-critical-fixes-rules-discovery/12-01-SUMMARY.md`
</output>
